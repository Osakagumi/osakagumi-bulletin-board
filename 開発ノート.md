# 大坂組社内ポータル 開発ノート

このドキュメントは、大坂組社内ポータルの開発を引き継ぐ方向けに、コードを読むだけでは分かりにくい「設計判断の理由」「仕様の経緯」「ハマりどころ」をまとめたものです。**このシステムはClaude（Anthropic社のAI）との対話で開発を進めてきました。今後の開発・保守もClaudeに相談しながら進めることを前提に、単なる記録に留めず「どう伝えればよいか」という指示の出し方も添えています。**

コード本体（`index.html` / `admin.html` / `firestore.rules`）と合わせて読んでください。このノートは仕様書ではなく、**コードの「なぜ」と「Claudeへの伝え方」を補うための資料**です。困ったときや大きな改修をする前に見返してください。

---

## 1. システム概要

- **名称**：大坂組社内ポータル（OSAKAGUMI INTRANET PORTAL）
- **公開URL**：`osakagumi.github.io/osakagumi-bulletin-board/`
- **画面は2つ**：
  - `index.html`：一般画面（全社員が使う）
  - `admin.html`：管理画面（権限を持つ人だけが使う）
- **形態**：ビルド工程のない、素のHTML+JavaScriptの静的サイト。フレームワーク（React等）は使っていません。

### 技術スタック

| 役割 | 技術 |
|---|---|
| ホスティング | GitHub Pages |
| 認証・DB | Firebase Authentication + Cloud Firestore |
| メール送信 | EmailJS（安否確認の通知メールのみ） |
| ファイル置き場 | Googleドライブ（業務情報・入札関連のExcelファイル。お知らせ・業務情報の添付アップロードはGoogle Apps Script経由、詳細は5章） |
| Excel出力・読込 | ExcelJS（ブラウザ内で完結、CDN経由） |
| 日本語エンコード変換 | encoding-japanese（過去に一時的に使用、現在は未使用） |

**サーバーサイドのコードは存在しません。** すべてブラウザ上のJavaScriptがFirestoreに直接読み書きしています。アクセス制御は「Firestore Security Rules」（`firestore.rules`）のみで行っています。ここが崩れるとセキュリティが崩壊するので、変更時は必ず整合性を確認してください。

---

## 2. データモデル（Firestoreコレクション一覧）

### 社員・権限まわり

| コレクション | キー | 主なフィールド | 備考 |
|---|---|---|---|
| `employees` | メールアドレス | name, employeeNo, departments(配列), department(旧・単一値、互換用), hiddenTabs(配列), order, phone, memo, hidden | 社員の本体データ |
| `admins` | メールアドレス | - | 存在＝システム管理者。ドキュメントの中身は空でよい（存在チェックのみ） |
| `announcement_posters` | メールアドレス | - | お知らせ投稿権限 |
| `whereabouts_editors` | メールアドレス | - | 行き先の他者設定権限 |
| `equipment_managers` | メールアドレス | - | 設備管理者（旧称：測量機器管理者） |
| `calendar_editors` | メールアドレス | - | カレンダー編集権限 |
| `contract_editors` | メールアドレス | - | 入札編集権限 |
| `safety_senders` | メールアドレス | - | 安否確認発信権限 |
| `blocked_users` | メールアドレス | blockedAt, blockedBy, name | 完全に削除された社員。ログインを強制ブロックする |
| `user_prefs` | メールアドレス | tabOrder, dashboardCardOrder, contractShowResolved等 | 個人ごとの画面設定（アカウント単位で永続化） |

**権限は「存在するかどうか」で判定する設計です。** `admins/foo@example.com` というドキュメントが存在すれば管理者、存在しなければ違う、という単純なチェックです。ドキュメントの中身（フィールド）は使っていません。

### 業務データ

| コレクション | 概要 |
|---|---|
| `departments` | 部署マスタ |
| `equipment_categories` | 設備カテゴリ（方式：`checkout`＝貸出管理／`inventory`＝在庫数管理／`timeline`＝時間枠予約） |
| `equipment` | 設備本体（測量機器・ドローン・敷鉄板・会議室・車両など、カテゴリ方式によってフィールドの意味が変わる） |
| `reservations` | 予約（会議室・車両などtimeline方式のカテゴリ用） |
| `announcements` | お知らせ |
| `notice_reads` | お知らせの既読管理（本人のみ読み書き可） |
| `direct_messages` | チャットDM（24時間で自動削除） |
| `contracts` | 入札情報 |
| `business_info_items` | 業務情報（Googleドライブのリンク集） |
| `safety_campaigns` / `safety_responses` | 安否確認 |
| `calendar_days` | カレンダー本体（**編集中のライブデータ**。編集権限者のみ読み書き想定だが、読み取りは全員可） |
| `calendar_days_published` | カレンダーの**公開済みスナップショット**（一般社員が実際に見るデータ。詳細は5章） |
| `calendar_years` | 年度ごとのメタデータ（`status: "draft"/"published"` を持つ） |
| `wbStatuses` / `wbPresets` | 行き先タブの選択肢マスタ |

### 設定値（`app_settings` コレクション）

| ドキュメントID | 内容 |
|---|---|
| `contract_defaults` | 入札対応部門の初期値 |
| `contract_cleanup` | 開札後の入札情報、自動削除タイミング |
| `contract_highlight` | 入札の直近ハイライト表示の日数（既定7日） |
| `notice_cleanup` | お知らせの自動削除までの日数（既定30日） |
| `safety_cleanup` | 安否確認の保存件数上限（既定5件） |

### システム全体のタブ既定値（`system_tab_defaults` コレクション、独立扱い）

| ドキュメントID | 内容 |
|---|---|
| `general` | 一般画面のタブ並び順・既定で有効にするタブ |
| `admin` | 管理画面のタブ並び順 |

`app_settings` と別コレクションにしたのは、**書き込み権限をシステム管理者のみに絞りたかったため**です（`app_settings` は複数の個別権限者にも書き込みを許可しているので、Firestoreルールの重複マッチの仕様上、同じコレクションに入れると意図せず権限が緩くなってしまいます）。

---

## 3. 権限モデルの設計思想

### 経緯

開発初期は、管理画面（`admin.html`）内の各タブに権限管理パネルが散らばっていました（「管理者管理」「お知らせ管理」の中にそれぞれ独立した権限追加フォームがある、という状態）。これを**「ユーザー管理」タブの社員編集モーダル内に統合**しました。理由は、権限の全体像が一箇所で見えないと、誰が何の権限を持っているか把握しづらくなるためです。

### 「システム管理者」と「個別権限」の関係

- **システム管理者**（`admins`コレクション）＝ すべての機能を無条件に操作できる、最上位の権限
- **個別権限**（お知らせ投稿権限、設備管理者など）＝ 特定の機能だけを操作できる権限

個別権限を持つ人は、**管理画面（`admin.html`）にログインできます。** ただし見えるのは、自分の権限に対応するタブだけです（例：入札編集権限だけを持つ人は「入札管理」タブしか見えない）。この「一部の機能だけ見せる」という設計にしたのは、社員数が多くない会社なので、権限ごとに別々の管理画面を作るよりも、1つの管理画面の中で表示を絞る方が保守しやすいと判断したためです。

管理画面の各セクションの権限判定は、`admin.html` 内の `ADMIN_SECTION_PERMISSIONS` という対応表で管理しています。新しく管理セクションを追加する際は、ここに追記してください。

```js
const ADMIN_SECTION_PERMISSIONS = {
  equipment:     { permission: "isEquipmentManager", generalTab: "checkout" },
  reservations:  { permission: null, generalTab: null },  // permission:null＝システム管理者専用
  ...
};
```

### タブの表示可否は「個人設定 AND システム設定」

これは開発の中盤で仕様変更した、やや複雑なルールです。

一般画面の各タブ（お知らせ、カレンダー、行き先など）は、以下の**2つの設定が両方とも「表示」の場合のみ**表示されます。

1. **個人設定**：`employees/{email}.hiddenTabs` 配列に、そのタブのIDが含まれていないか
2. **システム設定**：`system_tab_defaults/general.enabledTabs` 配列に、そのタブのIDが含まれているか

どちらか一方でも「非表示」なら、そのタブは非表示になります。これは、個人が「入札を見たい」と思っていても、システム管理者が組織全体で「入札機能を止める」と決めた場合はそちらが優先されるべき、という要望から生まれたルールです。

**重要**：この2つの設定の組み合わせルールは、開発当初「個人設定があればそちらを優先（システム設定は個人未設定の場合の初期値扱い）」という設計で一度実装し、その後「両方がYESの場合のみ表示（AND条件）」に仕様変更しています。もし今後また仕様を変える場合は、`index.html` の `applyMyHiddenTabs()` と `admin.html` の該当ロジック（`myHiddenTabsForAdmin` の計算部分）の**両方**を同時に直す必要があります。ここを片方だけ直すと、一般画面と管理画面で表示が食い違うバグになります（実際に一度発生しました）。

システム管理者は、この判定において特別扱いされません。システム設定でタブを無効化すれば、システム管理者であっても対応する管理画面のセクションは見えなくなります（「機能自体が全社で止まっているのに、管理者にだけ管理画面が見えても意味がない」という考え方です）。ただし「予約状況」「ユーザー管理」「システム管理」のように、そもそも一般画面に対応するタブがない管理専用セクションは、この判定の対象外（常にシステム管理者のみアクセス可）です。

**タブは実質3階層に分かれています（2026年8月、安否確認の追加で明確化）**：
1. **個人設定 AND システム設定の両方が対象**（`HIDEABLE_TABS`）：お知らせ・カレンダー・行き先・チャット・予約・機材・入札・業務情報。個人ごとの非表示設定にも、システム全体の既定値にも登場します。
2. **システム設定のみが対象**（`SAFETY_SYSTEM_TOGGLE_TABS`）：安否確認。緊急時に個人の設定でうっかり非表示にしてしまうと困るため、個人設定の対象からは意図的に除外し、全社一律のON/OFF（システム管理者のみ操作可）だけを許可しています。
3. **どちらの設定の対象にもならない（常時表示 or 別ロジック）**：ダッシュボードは常時表示。安否回答は、個人設定・システム設定とは無関係に「発信中の安否確認があるかどうか」だけで表示可否が決まります。

新しいタブを追加する際は、上記のどの階層に属するかを最初に決めてから実装してください。「システム設定のみ対象」にしたい場合は、`HIDEABLE_TABS`ではなく別定数に分離する必要があります（`HIDEABLE_TABS`に入れると、個人設定の対象にもなってしまいます）。

**表形式で表示タブを設定（マトリクス）の列/行一括トグル（2026年8月追加）**：`admin.html`「ユーザー管理」タブの「表形式で表示タブを設定」モーダルで、見出し（タブ名）をクリックすると列全体（全社員）、氏名をクリックすると行全体（全タブ）を一括でON/OFFできます。判定基準は「対象が全員（全タブ）表示中なら非表示へ、1つでも非表示があれば表示へ揃える」というトグル動作です。列の一括更新は社員数分のFirestore書き込みを並列実行するため、一部の書き込みだけ失敗した場合は失敗件数をトーストで表示します。

### 「タブの並び順」だけは、個人設定が完全に優先される別ルール

有効/無効のON-OFFとは違い、**並び順**については「個人が一度でも並び替えたら、以後はシステム既定値を一切無視して、個人設定を使い続ける」という単純な優先ルールです。個人が並び替えたことがない場合のみ、システム既定値の並び順が適用されます。

---

## 4. カレンダーの「編集中／公開中」の仕組み

これも開発後半で追加した、少し変わった設計です。理由から説明します。

### なぜこの仕組みが必要だったか

カレンダー編集権限者が、新年度の初期データを作ったり、既存年度を修正したりしている**作業の途中経過**が、一般社員にリアルタイムで見えてしまうのは望ましくない、という要望から生まれました。

### データの持ち方（2つのコレクションに分離）

- **`calendar_days`**：編集中のライブデータ。カレンダー編集権限者が実際に読み書きするのはここ
- **`calendar_days_published`**：公開済みスナップショット。**一般社員が見るのは常にこちら**

`calendar_years/{year}.status` が `"draft"`（編集中）か `"published"`（公開中）かを保持しています。ステータスの意味は「表示先の切り替え」には直接使っておらず、**社員が見るデータは常に `calendar_days_published`** です（ステータスに関わらず）。ステータスはあくまで「編集権限者への通知（今は未公開の変更がありますよ、というバッジ表示）」のために使っています。

### 動作の流れ

1. 編集権限者が日付をクリックして休日ON/OFFや名称を変更 → `calendar_days`（ライブ）が更新され、同時に該当年度の `status` が自動的に `"draft"` に戻る
2. 一般社員は `calendar_days_published`（最後に公開されたスナップショット）を見続けているので、編集中の変更には気づかない
3. 編集権限者が「公開する」ボタンを押す → その時点の `calendar_days` の内容を丸ごと `calendar_days_published` にコピーし、`status` を `"published"` に変更

### 例外：新規作成直後だけは即座に見える

年度の「初期データ作成」（`admin.html`）を実行した直後は、`status` は `"draft"` のままですが、**作成した内容をその場で `calendar_days_published` にも同時書き込みしています。** これをしないと、新規作成した年度は「公開する」を一度も押すまで一般社員には空のカレンダーが見えてしまうためです（実際にこの問題が起きて、後から対応しました）。

### バッジ表示の非対称性

- **カレンダー画面本体**：「公開中」「編集中」の両方を全社員に表示
- **ダッシュボードのカレンダーカード**：「編集中」の時だけバッジを出し、「公開中」の時は何も表示しない

これは意図的な仕様です（ダッシュボードは情報を絞って見せたいという要望）。

### 公開処理のパフォーマンス

「公開する」ボタンは、年度全体（最大366日分）のドキュメントを1件ずつ`setDoc`していたため、当初は体感で時間がかかっていました。現在は25件ずつ`Promise.all`でまとめて並列書き込みし、ボタンの文字に `公開中… 120/365` のように進捗を表示するようにしています。今後、日数が増えて遅くなるようであれば、バッチサイズ（`BATCH_SIZE`定数）を調整してください。

---

## 5. 主要機能ごとの補足

### 安否確認

- 発信・回答状況の管理は、**すべて一般画面（`index.html`）の「安否確認」タブに統合**されています。管理画面（`admin.html`）には安否確認関連のセクションはもう存在しません（開発初期はadmin.html側にありましたが、権限保有者が管理画面自体に入れないケースがあったため、一般画面へ全面移行しました）。
- 一般画面には「安否確認」タブ（発信・管理用、発信権限者のみ表示）と「安否回答」タブ（自分の回答用、発信中のみ全員に表示）の**2つのタブに分離**されています。最初は1つのタブにまとめていましたが、名称の紛らわしさから分離しました。
- メール内の回答リンクをクリックしてアクセスした場合、ログイン直後に自動的に「安否回答」タブが開き、メールで選んだ選択肢が事前に選択された状態になります。この自動遷移は、他の初期化処理（非表示タブの判定など）とタイミングが競合しやすいので、触る場合は注意してください（過去に実際に競合バグが起きました。`urlPreselect` 変数と `activeSafetyCampaign` の読み込み完了タイミングを揃える必要があります）。
- 発信件数の上限（既定5件）を超えたら、古いものから自動削除されます。件数は「安否確認」タブ内で設定できます。
- **「安否確認」タブのシステム全体ON/OFF（2026年8月追加）**：`admin.html`「システム管理」タブの「既定で有効にするタブ」に「安否確認」を追加しました。ここでチェックを外すと、発信権限者・システム管理者であっても全社一律で「安否確認」タブが非表示になります（運用しない組織向けの設定）。**ただし個人ごとの非表示設定（`HIDEABLE_TABS`／表形式で表示タブを設定マトリクス）には、あえて「安否確認」を含めていません**（緊急時に個人が誤って非表示にしてしまうのを防ぐため、という元々の設計意図をそのまま維持）。実装上は`SAFETY_SYSTEM_TOGGLE_TABS`という別定数（`admin.html`）と、`isGeneralTabSystemEnabled()`というヘルパー関数（`index.html`）で、既存の個人設定の仕組みとは独立させています。「安否回答」タブの表示条件（発信中のみ全員に表示）は、この設定と完全に独立しており、一切変更していません。

### お知らせ・業務情報の添付ファイル（Googleドライブ連携）

お知らせ・業務情報とも、添付ファイルは`storage-helper.js`（`index.html`・`admin.html`共通で読み込む）経由でGoogleドライブへアップロードします。仕組みは以下の通りです。

- 実体は`osakagumi.sys@gmail.com`名義で動作するGoogle Apps ScriptのWebアプリ（`apps-script-storage.gs`）で、ブラウザからはただのHTTP通信（`fetch`でPOST）として呼び出しているだけです。**利用者側でGoogleへのログインが発生することは一切ありません。** これはApps Script自体が、最初に一度だけ`osakagumi.sys@gmail.com`から権限をもらった状態で動く「代理」だからです。時間が経過しても、ファイル数が増えても、この点は変わりません。
- ファイルの実際の持ち主は引き続き`osakagumi.sys@gmail.com`のGoogleドライブです（Firebase Storageではありません）。`firebase-config.js`の`GAS_STORAGE_CONFIG`（`webAppUrl`・`secret`）が未設定・プレースホルダーのままだと、アップロード時にエラーメッセージが表示されます。
- 削除は「ゴミ箱に移動」で、30日後にGoogle側で自動的に完全削除されます（お知らせ添付ファイルの旧仕様「完全削除（ゴミ箱を経由しない）」から変更されています）。

**経緯・旧方式との違い**：以前は`gdrive-helper.js`という別ファイルで、ブラウザから直接Googleへログインする方式（Google Identity Services、`drive.file`スコープ）を使っていました。この方式では、アップロードのたびに（または時間経過後に）`osakagumi.sys@gmail.com`でのGoogleログイン確認が求められていました。2026年8月に`storage-helper.js`方式へ全面移行し、`gdrive-helper.js`は削除済みです。

> **Claudeへの伝え方（過去に実際にあった失敗）**：`uploadFileToStorage`という関数名だけを見て「Firebase Storageを使っているはず」と思い込み、中身を確認せずに誤った説明をしてしまったことがあります。ファイル名・関数名からの推測だけで仕組みを説明せず、`storage-helper.js`の中身を実際に読んでから答えるよう、念のため一言添えてください。

**お知らせのファイル添付UI（2026年8月、業務情報タブと同じ方式に統一）**：以前は投稿・編集モーダルに「リンク」テキスト欄＋「ファイル添付」ボタンがあり、ボタンを押すと別モーダルでドラッグ&ドロップ操作をする2段階の作り（かつリンクの生パスがテキスト欄に表示される）でした。業務情報タブの「追加」モーダルと同様、投稿・編集モーダルに直接ドラッグ&ドロップ欄を埋め込む方式に変更し、リンクの生パスは画面に一切表示しない（ファイル名のみ表示）ようにしています。この変更に伴い、以下の点に注意してください。
- 編集時に「リンク欄を空にして添付を外す」という操作ができなくなったため、代わりに編集モーダル専用の「添付を削除」ボタンを新設しています（新規投稿時は添付なしがデフォルトなので不要）。
- これまで`announcements`ドキュメントにはファイル名を保存しておらず（`attachmentUrl`のみ）、編集時に「今何が添付されているか」をリンクなしで示す手段がありませんでした。業務情報の`lastFileName`と同様に`attachmentFileName`フィールドを新設し、以後の投稿・編集からファイル名も保存するようにしています（過去のデータにはこのフィールドが無いため、編集時は「登録済みの添付ファイルがあります。」という汎用表示になります）。

**お知らせのメール通知は、実際にはEmailJSではなくmailto方式になっている**：`index.html`のお知らせ投稿・更新処理からは`window.emailjs.send`を一切呼び出しておらず、実際は`mailto:`リンクを100人ずつのバッチで開く方式（`openNoticeMailtoModal`関数）で送信されています（安否確認は今も`safetyTemplateId`でEmailJS自動送信のままです）。2026年8月の資産再確認時点では「いつ・なぜこの切り替えが行われたか記録がなく分からない」状態でしたが、ユーザーに確認したところ**意図的な変更**であることが確認できました。これに伴い、`firebase-config.js`の`EMAILJS_CONFIG.noticeTemplateId`（未使用のまま残っていた設定値）は削除済みです。EmailJSは現在、安否確認の1テンプレートのみで使用しています。

> **Claudeへの伝え方**：「お知らせのメール通知の実際の仕組みを確認して、READMEの記載と食い違っていないか確認して」と伝えると、今回のような実装とドキュメントの乖離を早期に発見しやすくなります。

### 業務情報タブ

- 一覧に表示されるのは、Googleドライブ上に置いたファイルへの共有リンクです。共有リンク経由でそのまま埋め込み表示しています（閲覧専用。`.xlsx`に限らず、Googleドライブの`/preview`エンドポイントが表示できる形式であれば種類を問いません）。
- **想定している運用**：ファイルの「本体」はPC上のローカルファイル（Excel等）で、日々そちらを更新し、更新するたびに管理画面から同じファイルを選び直してアップロードし直す、という使い方を前提にしています（Googleドライブ上やGoogleスプレッドシート上で直接編集する運用ではありません）。2026年8月にこの運用に合わせて仕様変更しました（旧仕様：Googleスプレッドシートの編集画面を直接開いて中身を編集する方式。今は廃止）。
- **新規ファイルの登録**：管理画面「業務情報管理」の「追加」モーダルに、名前入力欄とドラッグ&ドロップ/ファイル選択欄が直接並んでいます（別モーダルへの遷移なし）。アップロードは`storage-helper.js`方式（3章参照。ログイン確認は出ません）。Googleドライブのリンクをこの画面で手動入力する手段はもう用意していません（アップロード操作のみ）。
- **一覧のボタンは3種類**（表示順：ファイル修正 → 編集 → 削除）：
  - **ファイル修正**：登録済みファイルをGoogleスプレッドシートの編集画面で直接開きます（`https://docs.google.com/spreadsheets/d/{fileId}/edit` を新規タブで開くだけ）。2026年8月の仕様変更（ローカルファイルを選び直して上書きする運用への移行）の際に一度削除しましたが、既存機能の無断削除に当たるとしてユーザーから指摘を受け復元しました。以後、既存機能はそのまま残す方針です。
  - **編集**：名前・有効/無効に加え、追加時と同じドラッグ&ドロップ/ファイル選択欄も表示され、ファイルを選び直せば既存ファイルを置き換えられます（2026年8月の仕様変更で追加。それまでは名前・有効/無効のみの変更でした）。
  - **削除**：一覧から削除します（Googleドライブ側の実体も削除されます）。
- **「更新」ボタンは廃止済み（2026年8月）**：以前は「編集」とは別に、ファイルだけを選び直して上書きする専用の「更新」ボタンがありました。しかし「編集」モーダルにも同じファイル選び直し機能が追加されたことで、両者の役割が完全に重複したため、ユーザーの指示により「更新」ボタンを削除し、「編集」ボタンに一本化しています（このときは既存機能の重複が明確だったため、削除自体はユーザー自身の判断・指示によるものです。3章末尾「仕様変更のついでに、既存機能を無断で削除してしまったことがある」の教訓とは別ケースとして記録しておきます）。
- **「フルパス」ではなく「ファイル名」だけを記憶している**：当初「アップロードしたファイルのフルパスを記憶して、更新時に自動で読み直す」という要望がありましたが、ブラウザはセキュリティ上の理由でJavaScriptにファイルの絶対パスを一切渡しません（`<input type="file">`でも File System Access APIでも同様）。取得できるのはファイル名だけです。そのため実装は「ワンクリック再選択方式」（`lastFileName`フィールドにファイル名だけを記憶して一覧に表示し、「編集」ボタンを押すたびにファイル選択ダイアログを開いて選び直してもらう）にしています。
- **ダイアログの初期フォルダ・初期選択ファイルの自動設定は見送り**：同じ理由（ブラウザはファイル選択ダイアログの初期フォルダ・初期選択ファイルをJavaScriptから指定できない）で技術的に不可能なため、Chrome/Edge限定のFile System Access APIを使う代替案を提示しましたが、「現状のまま（標準の`<input type="file">`のみ）でよい」という判断になっています。ブラウザは直前に開いたフォルダを覚えていることが多いので、実際の手間はダイアログを開いてクリックするだけで済むことがほとんどです。

> **Claudeへの伝え方（今後似た要望が来たら）**：「アップロードしたファイルのフルパスを記憶して、ボタン一つで自動的に読み直したい」「ファイル選択ダイアログの初期フォルダ・ファイル名を自動設定したい」という依頼が来ることがありますが、いずれもブラウザの仕様上不可能です（ファイルの絶対パスはJavaScriptに渡されず、ダイアログの初期状態も指定できません）。同様の依頼が来たら、その旨を説明した上で、「ファイル名だけ記憶してワンクリックで選び直す方式」（または対応ブラウザを絞ったFile System Access API）を代替案として提示するよう伝えてください。

### 資機材タブ（旧称：資機材、現在は「機材」タブ）

- 測量機器・ドローン（貸出管理方式）と敷鉄板（在庫数管理方式）を、カテゴリのピル切り替えで表示します。
- 該当する設備が1件も登録されていない（または全て非表示設定の）カテゴリは、ピル自体を表示しません。

### CSV一括登録・エクスポート

- ユーザー登録・設備登録の両方でCSV一括登録に対応しています。列の1行目はヘッダーとして無視されます。
- 設備のCSV登録は、**名前と管理番号が一致する既存データがあれば新規作成ではなく更新する**仕様です（重複登録の防止。一度、同じCSVを2回インポートして重複データが大量発生する事故があり、対応しました）。運用中の情報（貸出状況・点検日など）はCSV側に含まれないため、更新時に上書きされません。

---

## 6. ハマりどころ・技術的な教訓

このシステムの開発中に実際に遭遇した不具合とその原因です。**それぞれに「Claudeへの伝え方」を添えています。** 同じ状況になったら、そのまま（または近い言い回しで）Claudeに伝えてください。Claudeは会話の最初にこのファイルを読んでいれば、以下の背景をふまえた対応をしてくれるはずですが、うまく伝わっていなさそうな場合は明示的に指示してください。

### VBScript（.vbs）はShift-JISで保存しないと文字化けでエラーになる

スタートアップ登録用にVBScriptを生成する機能を一時的に実装していました（現在は機能ごと削除済み）。日本語を含む`.vbs`ファイルをUTF-8で出力すると、Windows Script Hostが「文字が正しくありません」というコンパイルエラーを出すことがあります。UTF-8 BOM付きでも不完全で、最終的にはShift-JISへの変換ライブラリ（encoding-japanese）が必要でした。

> **Claudeへの伝え方**：「日本語を含むVBScript（.vbs）ファイルを生成する必要がある。Windows Script Hostで文字化けエラーが起きないよう、Shift-JISでエンコードして（UTF-8ではダメだった実績がある）」

### EmailJS・ExcelJSなどの外部CDNスクリプトは、必ず`async`属性を付ける

`<script>`タグでブロッキング読み込みにすると、iOS Safariで白画面（ブランク）になる不具合が発生しました。原因はCDNの読み込みが遅延・失敗した際に、ページ全体のレンダリングがブロックされてしまうためです。以後、外部スクリプトは全て`async`で読み込み、`emailjsReady`のようなフラグで読み込み完了を判定してから使う設計にしています。

> **Claudeへの伝え方**：「新しく外部CDNのライブラリを読み込む場合は、必ずasync属性を付けて。過去にブロッキング読み込みでiOS Safariが白画面になったことがある」
> もし実際にスマホで画面が真っ白になる不具合を報告された場合は：「iOS Safariで画面が真っ白になる。過去に外部スクリプトのブロッキング読み込みが原因だったことがあるので、その観点でも確認して」

### `node --check` だけでは構文エラーを見逃すことがある

CommonJSモードで動作するため、ESMスタイルのコード（`type="module"`のscriptタグ内の記述）を正しく検証できない場合があります。より確実なのはacornのESMアウェア構文解析です。

> **Claudeへの伝え方**：基本的にはClaude側が自律的にacornでのチェックも行うはずですが、心配な場合は「コードの構文チェックは、node --checkだけでなくacornでのESMパースも行って」と念押ししてください。

### 固定表示（スティッキー）ヘッダーと`scrollIntoView`の相性問題

ヘッダー・タブナビゲーションバーが画面上部に固定表示される設計のため、`element.scrollIntoView({block:"start"})` を使うと、対象要素がヘッダーの裏に隠れてしまいます。固定要素の高さぶんオフセットを引いてから`window.scrollTo`する必要があります。

> **Claudeへの伝え方**：「〇〇ボタンを押した時に指定した場所にスクロールしてほしいが、ヘッダーやタブバーに隠れないようにして」と、固定表示バーの存在を明示的に伝えると確実です。もし実際にボタンを押した際のスクロール位置がずれている（見出しの途中で止まる等）場合は、スクリーンショットを添えて「スクロール位置が中途半端。カード見出しが見えるようにしてほしい」のように具体的に伝えてください。

### CSS Gridの「行」ベースのレイアウトは、列ごとの高さが不揃いだと崩れて見える

複数カードを`display:grid; grid-template-columns:1fr 1fr`で並べた際、片方の列の内容が極端に長いと、もう片方の列の要素（ボタンなど）が「行」の高さに引きずられて意図しない位置に来ることがあります。

> **Claudeへの伝え方**：「このボタンの位置がおかしい（スクリーンショット添付）。列ごとに完全に独立した高さになるようレイアウトを直して」。位置の不具合はスクリーンショットを見せるのが一番早く伝わります。

### ダッシュボードのカード並び替え（ドラッグ&ドロップ）と、非カード要素の共存

ダッシュボードにボタン等の「カードではない要素」を追加する場合、ドラッグ&ドロップの並び替えロジックが、その非カード要素を追い越して末尾に配置してしまうことがあります。

> **Claudeへの伝え方**：「ダッシュボードに新しいボタン（カードではない要素）を追加したい。カードのドラッグ並び替え機能と干渉しないように」と、既存のドラッグ&ドロップ機能との共存を最初から意識してもらうと手戻りが減ります。

### admin.html初期表示タブの選定バグ（固定順序 vs 実際の表示順序）

ログイン後に「アクセスできる中で一番最初のタブ」を選ぶ処理で、JavaScriptのオブジェクトのキー順（コード上に書いた順）を使っていたため、実際の画面上のタブの並び順を無視して、常に固定の1つ目のタブがアクティブになってしまうバグがありました。

> **Claudeへの伝え方**：もし「並び替えたのに、画面を開くと違うタブがアクティブになる」といった現象が起きたら、「初期表示タブの選定ロジックが、実際の並び順ではなく固定の順序を見ていないか確認して」と伝えると、原因特定が早くなります。

### 権限まわりの新機能を追加したら、必ずFirestoreルールとの整合性を確認する

このプロジェクトでは、機能追加のたびにFirestoreルールの見直しが必要になる場面が何度もありました。画面側で機能を絞っても、Firestoreルール側が緩いままだと、URLを直接叩かれた場合などにセキュリティホールになります。

> **Claudeへの伝え方**：新しい権限・新しいコレクションに関わる機能を依頼する際は、必ず「Firestoreルールとの整合性も確認して」と一言添えてください。逆に、権限がらみの不具合（「〇〇の操作ができない／できてしまう」）を報告する際は、「画面側だけでなくfirestore.rules側の条件も見て」と伝えると、片方だけ直して終わる、という事故を防げます。

### Firestoreルールの「同じパスにマッチする複数のmatchブロック」は、権限が意図せず合算される

`match /app_settings/{settingId}` のような汎用ルールと、特定のドキュメントIDを指すルールを両方定義すると、Firestoreは両方の条件をORで評価するため、狭めたつもりの権限が実際には広いままになることがあります。

> **Claudeへの伝え方**：「この設定はシステム管理者だけが書き込めるようにしたいが、既存の`app_settings`コレクションだと他の権限者にも書き込みが開放されてしまわないか確認して。必要なら別コレクションに分離して」

### 機能を削除・置き換えたら、Firestoreルール側の「使われなくなったmatchブロック」も棚卸しする

お知らせ添付の削除処理を、旧方式（`pending_drive_deletes`コレクションに削除待ちを積んでおく非同期方式）から、`storage-helper.js`経由の同期的な削除呼び出しに変更した際、`firestore.rules`側の`pending_drive_deletes`用のmatchブロックを消し忘れていました。実害はありません（もう誰も書き込まないコレクションへの権限が残っているだけ）が、ルールファイルが実態より複雑になり、後から読む人を混乱させます。

> **Claudeへの伝え方**：「〇〇の実装方式を変えた（または機能を削除した）ので、firestore.rulesの中に、もう使われていないコレクションへのmatchブロックが残っていないか確認して」と、機能変更のたびに一言添えると、ルールファイルの棚卸し漏れを防げます。

### 画面ごと機能を移行すると、旧画面側のJavaScriptだけが「死んだコード」として残ることがある（2026年8月発見・未対応）

安否確認の発信・管理機能は、開発初期の`admin.html`「安否確認管理」タブから、現在は`index.html`「安否確認」タブへ全面移行済みです（5章参照）。しかし2026年8月に全アセットを再確認した際、`admin.html`側に**HTML（ナビボタン・`id="view-safety"`等）は一切残っていないにもかかわらず、対応するJavaScript（`initSafetyCampaignsListener`・`renderSafetyCampaignSelect`・`renderSafetyDashboard`・`sendSafetyEmails`・`safety-launch-btn`等のイベントリスナー、およそ260行）だけがそのまま残っている**ことが判明しました。存在しないDOM要素を`document.getElementById("safety-...")?.addEventListener(...)`のようにオプショナルチェイニングで参照しているため、エラーにはならず実害はありませんが、実際には一切呼び出されない「死んでいるが動いているように見えるコード」になっています。**この記録を書いた時点では、ユーザーへの確認待ちで未削除のままです**（3章末尾の「既存機能を勝手に削除するのは絶対禁止」という方針があるため、死んでいると判断はできても、確認前に自己判断で削除していません）。

> **Claudeへの伝え方**：機能を別画面へ全面移行する依頼を受けた際は、移行元の画面のHTML・JSの両方を洗い出し、「HTMLの対応する要素が本当に消えているか」「JSが他の生きているUIから一切呼ばれていないか」を`grep`等で確認したうえで、まとめて削除してよいか確認してください。今回のように「HTMLだけ消してJSを消し忘れる」（またはその逆）というズレは、今後も起こり得ます。「〇〇の機能を△△画面に移行して」と依頼する際は、「移行元に使われなくなったコードが残らないよう、最後に棚卸しして」と一言添えると確実です。

### 仕様変更のついでに、既存機能を無断で削除してしまったことがある

業務情報管理を「ローカルファイルを選び直して上書きする」運用に仕様変更した際、Claudeが「新しい運用と役割が重複するから」と自己判断で、既存の「ファイル修正」ボタン（Googleスプレッドシートの編集画面を直接開く機能）を、確認なしに削除してしまったことがありました。ユーザーから強く指摘を受け、その場で復元しています。

> **Claudeへの伝え方**：「既存の機能を勝手に削除するのは絶対禁止。削除してよいか確認が必要な場合は、必ず『〇〇の機能は削除してもいい？』と聞いてから対応して」と伝えてください。この方針はこのプロジェクトに限らず、Claudeの永続的な行動ルールとして記憶させてあります。仕様変更を依頼する際、既存機能との重複や矛盾が疑われる場合は、Claude側から「〇〇という既存機能がありますが、これは削除してよいですか？　それとも残しますか？」と先に確認を挟むのが正しい振る舞いです。

---

## 7. Claudeに開発を依頼する際の、効果的な指示の出し方

このシステムの開発は、最初から仕様が固まっていたわけではなく、**「まず動くものを見て→直したい点を伝えて→また見る」を繰り返す進め方**でした。この進め方を踏まえて、指示の出し方のコツをまとめます。

### 不具合報告は、スクリーンショットを添えるのが一番早い

「ボタンの位置がおかしい」「タブがちゃんと表示されない」といった見た目の不具合は、文章だけで説明するより**画面のスクリーンショットを貼って一言添える**方が、Claudeが正確に状況を理解できます。実際にこのプロジェクトでも、スクリーンショット付きの報告から「ヘッダーに隠れている」「列の高さに引きずられている」といった根本原因を特定できたケースが何度もありました。

### 大きな仕様変更は、いきなり「直して」ではなく「修正候補を列挙して」から

ボタンラベルの統一や、権限まわりの大きな見直しなど、影響範囲が広い変更を依頼する際は、まず「〇〇について、修正が必要そうな箇所を洗い出してリストにして（まだ直さなくていい）」と依頼し、リストを確認してから「①②は採用、③は見送り」のように取捨選択して進めると、意図しない箇所まで変更されてしまう事故を防げます。

### 「全コードを確認して」と言えば、機能コードだけでなく説明文・コメント・UI文字列も対象になる

このプロジェクトでは、「〇〇という名称を△△に変更して。全コードを確認して該当箇所をすべて修正して」という依頼を何度か行っています。この言い方をすると、Claudeは画面上のボタンラベルだけでなく、確認ダイアログの文言、コード内コメント、CSVのヘッダーなど、**関連する全ての文字列を洗い出して直します。** 逆に「ボタンの名前だけ変えて」のように範囲を絞りたい場合は、その旨を明示してください。

### 名称変更・機能移動の際は「別の意味で使われている箇所」に注意してもらう

例えば「資機材」→「機材」への変更や、「管理者」→「システム管理者」への変更では、**単純な文字列の一括置換では済まない**（「測量機器管理者」という別の権限名まで巻き込んでしまう等）ケースがありました。紛らわしい名称変更を依頼する際は、「〇〇という言葉を△△に変えたいが、他の意味で使われている箇所（例：□□）は変えないで」のように、除外すべきケースを事前に伝えると、より正確に対応できます。

### 権限・表示条件の変更は、条件を具体例で伝えると誤解が起きにくい

「AとBの両方を満たす場合だけ表示する」といった条件のロジックは、文章の言い回しだけだと解釈がずれることがあります。このプロジェクトでは「入札(個人設定)＝表示、入札（システム設定）＝非表示　の場合は入札タブは非表示とし、入札(個人設定)＝非表示、入札（システム設定）＝表示　の場合も入札タブは非表示とする」のように、**具体的な組み合わせパターンを列挙して**伝えることで、認識のズレを防いでいます。複雑な条件分岐を依頼する際は、この書き方をおすすめします。

### 「他に設定すべき項目はあるかな？」と壁打ちに使うのも有効

新しい設定機能を作る前に、「〇〇を設定できるようにしたい。他にこの画面で設定すべき項目はあるかな？」とClaudeに聞いてから機能を確定させる進め方も、このプロジェクトで何度か行っています。Claudeは既存コードを検索して「今コードに直接埋め込まれている値」を洗い出して提案してくれるので、機能の抜け漏れ防止に有効です。

### 「実装の中身、本当に確認した？」と念押しするのも有効

過去に一度、Claudeが関数名（`uploadFileToStorage`）だけから実装方式を推測し、実際のファイル中身を確認しないまま誤った説明をしたことがありました。特に「なぜこの仕様になっているか」「本当にこの制約があるか」を聞く場面では、「関数名や過去の説明からの推測ではなく、実際にファイルの中身を開いて確認してから答えて」と一言添えると、この種の思い込みを防げます。

### 修正が終わったら、Claudeは自分でファイルを検証してから提示する

このプロジェクトのClaudeとのやり取りでは、コード修正のたびに構文チェック（node --check・acornパース・重複ID確認・CSSの波括弧対応など）を自分で行ってから成果物を提示する、という進め方が徹底されています。**もし提示された説明の中にこの検証工程が見当たらない場合は、「検証してから出して」と伝えてください。**

---

## 8. コーディング規約・命名の慣習

- **ボタンのラベルは体言止め**（動詞の「〜する」を付けない）。「保存する」ではなく「保存」。ただし文脈依存の丁寧な言い回し（「予約を確定する」「回答する」等、状態に応じて文言が変わるボタン）はこの限りではありません。
- **「＋」記号はボタンラベルに使わない**。「＋追加」ではなく「追加」。
- 権限名は「システム管理者」「設備管理者」のように統一（旧称「管理者」「測量機器管理者」からの改名。単体の「管理者」という言葉が出てきたら「システム管理者」を指すが、「測量機器管理者」のような複合語は別概念なので改名の対象外）。
- コード内コメントは日本語。特に「なぜこの実装にしたか」という背景を残す傾向があります（後から見返す際に役立つため、これは踏襲することをおすすめします）。
- Firestoreへの書き込みは、失敗時に`toast("〜に失敗しました。権限をご確認ください。")`という定型文で通知するパターンが徹底されています。

---

## 9. 引き継ぎにあたっての注意

- **このシステムは、まだ社内に正式公開されていません**（開発中）。そのため、過去のやり取りの中には「今は権限案内のカードを消してもよい」といった、公開後には通用しない判断も含まれています。公開後に大きな仕様変更をする際は、既存ユーザーへの影響（画面が急に変わる、データが消える等）をより慎重に検討してください。
- Firestoreのデータそのものは、このノートには含まれていません。実際の部署名・カテゴリ名・社員データ等は、Firebaseコンソールで直接確認してください（③のプラットフォーム入門ドキュメントを参照）。
- 迷ったときは、まずこのシステムのプロジェクト知識としてアップロードされているコード（`index.html`, `admin.html`, `firestore.rules`）と、このノートを両方読み込んだ上でClaudeに質問するのがおすすめです。過去の判断の経緯を踏まえた回答が得られやすくなります。
- **このプロジェクトの知識ベース（`index.html`等）は、都度GitHubリポジトリ（`osakagumi/osakagumi-bulletin-board`）の最新版と同期する運用にしています。** 会話の中でコードの実体を確認する必要があるときは、まずプロジェクト知識を確認し、更新が疑わしい場合はGitHubの最新版と突き合わせるようClaudeに伝えてください。
